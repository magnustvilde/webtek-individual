1. What is the difference between position: absolute and position: fixed?
Answer: Their position is relative to, respectively, the nearest ancestor and the viewport.
"absolute" will use the body as its ancestor if nothing else is found to be its ancestor.
2. With CSS3, how do we access the third element within another element?
Answer: element > element > :nth-child(3)
